@model IEnumerable<dynamic>

@foreach (var item in Model)
{
    <tr>
        <td>
            <input type="checkbox" name="selectedExpertIds" value="@item.Id" class="exp-select" />
        </td>
        <td>@item.Name</td>
        <td>@item.Surname</td>
        <td>@item.FinCode</td>
    </tr>
}
